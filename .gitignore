# Dependencies
node_modules/

# Build output
dist/
*.zip
neon-drift.zip

# Local Claude Code config
.claude/

# OS / editor
.DS_Store
Thumbs.db
*.log

# Visual-check screenshots (regenerated by scripts/visual-check.mjs)
scripts/_shots/

# Firebase config — contains API key; never commit to public repo.
# Copy firebase-config.example.js → firebase-config.js and fill in real values.
src/firebase-config.js

# Submission screenshots (large PNGs, regenerated by make-screenshots.mjs)
scripts/_submit/
